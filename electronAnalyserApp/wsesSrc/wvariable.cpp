#include "wvariable.hpp"

/*! \class WVariable
 * \brief This class works as a controller for the SESWrapper variables.
 *
 * The main member functions are set() and get() which act as class callbacks through the
 * Command design pattern [Gamma et. al.].
 *
 * \see WSESWrapperBase
 */
